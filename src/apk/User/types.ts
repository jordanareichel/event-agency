export type User = {
  name: string;
  userMail: string;
  gender: string;
  phone?: string;
  zip: string;
  state: string;
  city: string;
  address: string;
  number?: number;
  userPassword: string;
  confirmPassword: string;
};
